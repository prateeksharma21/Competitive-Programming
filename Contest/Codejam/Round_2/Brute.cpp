#ifdef Prateek
  #include "Prateek.h"
#else
  #include <bits/stdc++.h>
  using namespace std;
  #define debug(...) 42
#endif
#define int ll
#define all(c) begin(c),end(c)
using ll = long long;
const int MOD = 1e9+7, N = 1e3 + 10; 

int dp[N][N];

int32_t main(){
   ios::sync_with_stdio(false);
   cin.tie(NULL);
   for (int i = 3; i < N; ++i) {
      for (int j = 3; j <= i; ++j) {
         for (int k = 3; k < N; ++k) {
            if (dp[])
         }
      }
   }
   return 0;
}